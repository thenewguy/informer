box: wercker/python
build:
  steps:
    - pip-install:
        requirements_file: "container/requirements.txt"

    - script:
        name: run unit tests
        code: py.test

    - script:
        name: pypi deploy
        code: twine upload -u $PYPI_USER -p $PYPI_PASSWORD dist/*
